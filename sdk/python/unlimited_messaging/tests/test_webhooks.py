# This file was auto-generated by Fern from our API Definition.

from unlimited_messaging import UnlimitedMessagingApi
from unlimited_messaging import AsyncUnlimitedMessagingApi
import typing
from .utilities import validate_response


async def test_webhook_endpoint_list(
    client: UnlimitedMessagingApi, async_client: AsyncUnlimitedMessagingApi
) -> None:
    expected_response: typing.Any = {
        "data": [
            {
                "id": "id",
                "url": "url",
                "description": "description",
                "secretPrefix": "secretPrefix",
                "enabled": True,
                "eventTypes": ["message.received"],
                "createdAt": "2024-01-15T09:30:00Z",
                "updatedAt": "2024-01-15T09:30:00Z",
            }
        ]
    }
    expected_types: typing.Any = {
        "data": (
            "list",
            {
                0: {
                    "id": None,
                    "url": None,
                    "description": None,
                    "secretPrefix": None,
                    "enabled": None,
                    "eventTypes": ("list", {0: None}),
                    "createdAt": "datetime",
                    "updatedAt": "datetime",
                }
            },
        )
    }
    response = client.webhooks.webhook_endpoint_list()
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.webhooks.webhook_endpoint_list()
    validate_response(async_response, expected_response, expected_types)


async def test_webhook_endpoint_create(
    client: UnlimitedMessagingApi, async_client: AsyncUnlimitedMessagingApi
) -> None:
    expected_response: typing.Any = {
        "id": "id",
        "url": "url",
        "description": "description",
        "secretPrefix": "secretPrefix",
        "enabled": True,
        "eventTypes": ["message.received"],
        "createdAt": "2024-01-15T09:30:00Z",
        "updatedAt": "2024-01-15T09:30:00Z",
        "secret": "secret",
    }
    expected_types: typing.Any = {
        "id": None,
        "url": None,
        "description": None,
        "secretPrefix": None,
        "enabled": None,
        "eventTypes": ("list", {0: None}),
        "createdAt": "datetime",
        "updatedAt": "datetime",
        "secret": None,
    }
    response = client.webhooks.webhook_endpoint_create(
        url="url", event_types=["message.received"]
    )
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.webhooks.webhook_endpoint_create(
        url="url", event_types=["message.received"]
    )
    validate_response(async_response, expected_response, expected_types)


async def test_webhook_endpoint_find_one(
    client: UnlimitedMessagingApi, async_client: AsyncUnlimitedMessagingApi
) -> None:
    expected_response: typing.Any = {
        "id": "id",
        "url": "url",
        "description": "description",
        "secretPrefix": "secretPrefix",
        "enabled": True,
        "eventTypes": ["message.received"],
        "createdAt": "2024-01-15T09:30:00Z",
        "updatedAt": "2024-01-15T09:30:00Z",
    }
    expected_types: typing.Any = {
        "id": None,
        "url": None,
        "description": None,
        "secretPrefix": None,
        "enabled": None,
        "eventTypes": ("list", {0: None}),
        "createdAt": "datetime",
        "updatedAt": "datetime",
    }
    response = client.webhooks.webhook_endpoint_find_one(id="id")
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.webhooks.webhook_endpoint_find_one(id="id")
    validate_response(async_response, expected_response, expected_types)


async def test_webhook_endpoint_delete(
    client: UnlimitedMessagingApi, async_client: AsyncUnlimitedMessagingApi
) -> None:
    # Type ignore to avoid mypy complaining about the function not being meant to return a value
    assert (
        client.webhooks.webhook_endpoint_delete(id="id")  # type: ignore[func-returns-value]
        is None
    )

    assert (
        await async_client.webhooks.webhook_endpoint_delete(id="id")  # type: ignore[func-returns-value]
        is None
    )


async def test_webhook_endpoint_update(
    client: UnlimitedMessagingApi, async_client: AsyncUnlimitedMessagingApi
) -> None:
    expected_response: typing.Any = {
        "id": "id",
        "url": "url",
        "description": "description",
        "secretPrefix": "secretPrefix",
        "enabled": True,
        "eventTypes": ["message.received"],
        "createdAt": "2024-01-15T09:30:00Z",
        "updatedAt": "2024-01-15T09:30:00Z",
    }
    expected_types: typing.Any = {
        "id": None,
        "url": None,
        "description": None,
        "secretPrefix": None,
        "enabled": None,
        "eventTypes": ("list", {0: None}),
        "createdAt": "datetime",
        "updatedAt": "datetime",
    }
    response = client.webhooks.webhook_endpoint_update(id="id")
    validate_response(response, expected_response, expected_types)

    async_response = await async_client.webhooks.webhook_endpoint_update(id="id")
    validate_response(async_response, expected_response, expected_types)
